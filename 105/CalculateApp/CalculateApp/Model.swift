//
//  Model.swift
//  CalculateApp
//
//  Created by Mac on 2023-10-09.
//

/*
import UIKit

class Operation {
    var currentOperationType: OperationType = OperationType.noOperation
    var currentNumber = "0"
    var firstNumber: Double = 0.0
    var secondNumber: Double = 0.0
    var result = ""
}

class Model {
    
    var currentOperationType: OperationType = OperationType.noOperation
    var currentNumber = "0"
    var firstNumber: Double = 0.0
    var secondNumber: Double = 0.0
    var result = ""
    
    private func makeCalculation(operation: OperationType) {
        if currentOperationType != .noOperation {
            secondNumber = Double(currentNumber) ?? 0.0
            
        } else {
            firstNumber = Double(currentNumber) ?? 0.0
            currentNumber = ""
            currentOperationType = operation
        }
    }
    
    private var history: [Operation]?
}

*/
